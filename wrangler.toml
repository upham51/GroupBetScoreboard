name = "group-bet-scoreboard"
compatibility_date = "2026-09-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

# SUPABASE_URL and SUPABASE_ANON_KEY are deliberately absent from this file.
# Set them as environment variables in the Cloudflare Pages project settings
# (Settings -> Environment variables) for both Production and Preview.
# For local development, put them in an untracked .dev.vars file.
